# App Router Migration — Wave 2.5: Header + Footer (Layout Chrome)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать city-scoped App Router страницам **рабочий desktop Header (logo, городской selector, телефон, language switcher, login button placeholder) + Footer (App Stores, меню, информация, соцсети, copyright)**. Это разблокирует Wave 2B (статичные страницы contacts/delivery/branch/privacy/fran) и сделает `/[city]/about` визуально соответствующим продакшену.

**Architecture:** Создаются App Router-compatible копии Header/Footer/sub-components (с суффиксом `App`). Заменяется: `useRouter` from `next/router` → `useRouter`/`usePathname` from `@/i18n/navigation`; `useTranslation` from `next-translate` → `useExtracted` from `next-intl` (NEVER `useTranslations`). Footer — server component (статика + props). Header — client component (модалки, dropdowns, локализация). `app/[city]/layout.tsx` server-side fetch'ит `siteInfo` + `publicConfig` и передаёт через props.

**Out of scope (отдельные waves):**
- `LocationTabs` (1557 строк, Leaflet карты) → Wave 2.5b
- `SignInModal` полный flow (850 строк, OTP auth) → Wave 2.5c — пока SignInButtonApp = placeholder без модалки
- `MobileHeader`, `MobileBottomNav`, `MobLocationTabs`, `MobChooseCityDropDown` (mobile UX) → Wave 2.5d
- `BonusModal`, `CityModal` → отдельные части по необходимости
- `HeaderMenu` (раскрытое меню по клику menuIcon) → Wave 3+

**Tech Stack:** Next.js 16.2.2, React 18, TypeScript 5.3.3, `next-intl@4.9.1` (`useExtracted` only — `useTranslations` strictly forbidden), `@headlessui/react` (Menu, Transition), `@heroicons/react`, `libphonenumber-js`, `next/image`, `next/script`, `js-cookie`.

**Reference spec:** `docs/superpowers/specs/2026-04-22-app-router-migration-design.md`
**Reference plans:** `2026-04-22-wave1-foundation.md`, `2026-04-22-wave2-static-pages.md`

---

## File Structure

### Created

| Файл | Ответственность |
|---|---|
| `lib/data/configs.ts` | `fetchPublicConfig()` — server fetch `/api/configs/public` (footer workTime), unstable_cache |
| `components_new/header/HeaderPhoneApp.tsx` | `'use client'`: телефон с `useUI()` (city.phone) — без useRouter/useTranslation |
| `components_new/header/ChooseCityDropDownApp.tsx` | `'use client'`: города dropdown с `useRouter`/`usePathname` из `@/i18n/navigation`, `useExtracted` |
| `components_new/header/LanguageDropDownApp.tsx` | `'use client'`: language dropdown с `useRouter` из `@/i18n/navigation` (использует `replace` для смены locale через cookie) |
| `components_new/header/SignInButtonApp.tsx` | `'use client'`: placeholder кнопка "Войти" — открывает `useUI().openSignInModal()` (модалка пока не рендерится в App Router; полный flow в Wave 2.5c) |
| `components_new/HeaderApp.tsx` | `'use client'`: main desktop Header (logo + ChooseCityDropDownApp + HeaderPhoneApp + LanguageDropDownApp + SignInButtonApp) |
| `components_new/FooterApp.tsx` | Server component: Footer (App Store badges, menu, информация, соцсети, copyright) — рендерится из props |

### Modified

| Файл | Изменение |
|---|---|
| `app/[city]/layout.tsx` | Server: добавить fetch `publicConfig`, передать `topMenu`/`footerInfoMenu`/`socials`/`categories`/`config` в LayoutWrapper |
| `app/[city]/LayoutWrapper.tsx` | Заменить `<main>{children}</main>` → `<HeaderApp menu={topMenu}/> <main>{children}</main> <FooterApp config={config} categories={categories} ... />` |

### Untouched (waves далее)

- `components_new/header/SetLocation.tsx`, `LocationTabs.tsx`, `MobLocationTabs..tsx` (Wave 2.5b — Leaflet карты)
- `components_new/header/SignInModal.tsx`, `UserProfileDropDown.tsx`, `AuthModal.js` (Wave 2.5c — auth flow)
- `components_new/header/HeaderMenu.tsx` (Wave 3+ — раскрытое меню)
- `components_new/mobile/*` (Wave 2.5d — mobile UX)
- Legacy `components_new/Header.tsx`, `components/common/Layout/Layout.tsx` — остаются для Pages Router до Wave 6 cleanup

---

## Pre-flight

- [ ] **Step 1: Verify Wave 2A done**

Run:
```bash
cd /Users/macbookpro/development/next_frontend
git branch --show-current  # = migration/app-router
git log --oneline -5
ls "app/[city]/about/page.tsx" components_new/about/AboutApp.tsx
```

Expected: ветка `migration/app-router`, `app/[city]/about/page.tsx` и `AboutApp.tsx` существуют, последний commit ~ `79d78ae3` (fix(app): minimal city layout) или newer.

- [ ] **Step 2: Baseline build OK**

Run:
```bash
rm -rf .next && bun run build > /tmp/wave2.5-baseline.log 2>&1 && echo "OK" || (echo "FAIL"; tail -30 /tmp/wave2.5-baseline.log)
```

Expected: `OK`.

---

## Task 1: lib/data/configs.ts

**Files:** Create `lib/data/configs.ts`

Server-side cached fetcher для `/api/configs/public` (использует Footer и Header для workTime). Заменяет client-side axios + sessionStorage logic из legacy `Layout.tsx`/`Header.tsx`/`Contacts.tsx`.

- [ ] **Step 1: Создать файл**

Write `lib/data/configs.ts`:

```typescript
import 'server-only'
import { unstable_cache as cache } from 'next/cache'

export type PublicConfig = {
  workTimeRu?: string
  workTimeUz?: string
  workTimeEn?: string
  [key: string]: unknown
}

async function fetchPublicConfigRaw(): Promise<PublicConfig> {
  const apiUrl = process.env.NEXT_PUBLIC_API_URL || process.env.API_URL
  const res = await fetch(`${apiUrl}/api/configs/public`, {
    next: { revalidate: 3600, tags: ['public-config'] },
  })
  if (!res.ok) throw new Error(`Failed to fetch public config: ${res.status}`)
  const json = (await res.json()) as { data: string }
  const decoded = Buffer.from(json.data, 'base64').toString('utf8')
  return JSON.parse(decoded) as PublicConfig
}

export const fetchPublicConfig = cache(fetchPublicConfigRaw, ['public-config'], {
  revalidate: 3600,
  tags: ['public-config'],
})
```

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^lib/data/configs" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add lib/data/configs.ts
git commit -m "feat(data): add fetchPublicConfig (footer/header workTime, 1h cache)"
```

---

## Task 2: HeaderPhoneApp.tsx

**Files:** Create `components_new/header/HeaderPhoneApp.tsx`

Identical к legacy `HeaderPhone.tsx` — uses `useUI()`, `libphonenumber-js`. Никаких useRouter/useTranslation. Просто copy + переименование default export.

- [ ] **Step 1: Создать файл**

Write `components_new/header/HeaderPhoneApp.tsx`:

```typescript
'use client'

import { useUI } from '@components/ui/context'
import { PhoneIcon } from '@heroicons/react/solid'
import { FC, memo, useMemo } from 'react'
import parsePhoneNumber from 'libphonenumber-js'

const HeaderPhoneApp: FC = () => {
  const { activeCity, cities } = useUI()

  const chosenCity = useMemo(() => {
    if (activeCity) return activeCity
    if (cities) return cities[0]
    return null
  }, [cities, activeCity])

  return (
    <div className="flex items-center mx-3">
      {chosenCity?.phone && (
        <div className="space-x-1 flex items-center">
          <PhoneIcon className="w-5 h-5 text-secondary" />
          <div>
            <a
              href={parsePhoneNumber(chosenCity?.phone)?.getURI()}
              className="text-secondary"
            >
              {parsePhoneNumber(chosenCity?.phone)
                ?.formatNational()
                .substring(2)}
            </a>
          </div>
        </div>
      )}
    </div>
  )
}

export default memo(HeaderPhoneApp)
```

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/header/HeaderPhoneApp" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add components_new/header/HeaderPhoneApp.tsx
git commit -m "feat(header): add HeaderPhoneApp (App Router copy of HeaderPhone)"
```

---

## Task 3: ChooseCityDropDownApp.tsx

**Files:** Create `components_new/header/ChooseCityDropDownApp.tsx`

Городской selector dropdown. **Изменения vs legacy:**
- `useRouter`/`router.push` from `next/router` → `useRouter`/`usePathname` from `@/i18n/navigation`
- `useTranslation('common').t('your_city')` → `useExtracted()('Ваш город')`
- `query.city` → читаем from `usePathname()` (заменяем `[city]` → новый slug)
- Локалью `useLocale()` from `next-intl` (вместо `router.locale`)

- [ ] **Step 1: Создать файл**

Write `components_new/header/ChooseCityDropDownApp.tsx`:

```typescript
'use client'

import { Fragment, FC, memo, useMemo } from 'react'
import {
  Menu,
  MenuButton,
  MenuItem,
  MenuItems,
  Transition,
} from '@headlessui/react'
import { useUI } from '@components/ui'
import type { City } from '@commerce/types/cities'
import Cookies from 'js-cookie'
import { useExtracted, useLocale } from 'next-intl'
import { useRouter, usePathname } from '../../i18n/navigation'

const ChooseCityDropDownApp: FC = () => {
  const t = useExtracted()
  const locale = useLocale()
  const router = useRouter()
  const pathname = usePathname()
  const { cities, activeCity, setActiveCity } = useUI()

  const chosenCity = useMemo(() => {
    if (activeCity) return activeCity
    if (cities) return cities[0]
    return null
  }, [cities, activeCity])

  const changeCity = (city: City) => {
    // pathname от next-intl/navigation возвращает path БЕЗ locale prefix.
    // Меняем первый сегмент (текущий city slug) на выбранный slug.
    const segments = pathname.split('/').filter(Boolean)
    if (segments.length > 0 && chosenCity?.slug && segments[0] === chosenCity.slug) {
      segments[0] = city.slug
    } else {
      segments.unshift(city.slug)
    }
    const newPath = '/' + segments.join('/')
    router.push(newPath)
    setActiveCity(city)
    Cookies.set('city_confirmed', '1', { expires: 365 })
    Cookies.set('city_slug', city.slug, { expires: 365 })
  }

  if (!chosenCity) return null

  return (
    <Menu as="div" className="relative inline-block text-left">
      {({ open }) => (
        <>
          <div>
            <MenuButton className="bg-white focus:outline-none font-medium inline-flex justify-center outline-none text-secondary text-2xl w-full">
              {locale === 'uz' ? chosenCity?.name_uz : ''}
              {locale === 'ru' ? chosenCity?.name : ''}
              {locale === 'en' ? chosenCity?.name_en : ''}
            </MenuButton>
          </div>

          <Transition
            show={open}
            enter="transition ease-out duration-100"
            enterFrom="transform opacity-0 scale-95"
            enterTo="transform opacity-100 scale-100"
            leave="transition ease-in duration-75"
            leaveFrom="transform opacity-100 scale-100"
            leaveTo="transform opacity-0 scale-95"
          >
            <MenuItems className="absolute bg-white divide-gray-100 divide-y focus:outline-none mt-2 origin-top-right right-0 ring-1 ring-black ring-opacity-5 rounded-2xl shadow-lg top-0 z-20">
              <MenuItem>
                {() => (
                  <span className="text-secondary block px-4 py-2">
                    {t('Ваш город')}
                  </span>
                )}
              </MenuItem>
              {cities?.map((item: City) => (
                <MenuItem key={item.id}>
                  <span
                    onClick={() => changeCity(item)}
                    className={`block px-4 py-2 cursor-pointer ${
                      chosenCity.id === item.id
                        ? 'bg-secondary text-white'
                        : 'text-secondary'
                    }`}
                  >
                    {locale === 'uz' ? item.name_uz : ''}
                    {locale === 'ru' ? item.name : ''}
                    {locale === 'en' ? item.name_en : ''}
                  </span>
                </MenuItem>
              ))}
            </MenuItems>
          </Transition>
        </>
      )}
    </Menu>
  )
}

export default memo(ChooseCityDropDownApp)
```

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/header/ChooseCityDropDownApp" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add components_new/header/ChooseCityDropDownApp.tsx
git commit -m "feat(header): add ChooseCityDropDownApp (next-intl + useExtracted, no useRouter from pages)"
```

---

## Task 4: LanguageDropDownApp.tsx

**Files:** Create `components_new/header/LanguageDropDownApp.tsx`

Language dropdown. Текущий `LanguageDropDown` использует `router.push(link, link, { locale })` — Pages Router API.

В нашем setup (Wave 1: `localePrefix: 'never'`) локаль определяется через `NEXT_LOCALE` cookie. Изменение языка — set cookie + reload.

- [ ] **Step 1: Создать файл**

Write `components_new/header/LanguageDropDownApp.tsx`:

```typescript
'use client'

import { FC, memo } from 'react'
import {
  Menu,
  MenuButton,
  MenuItem,
  MenuItems,
  Transition,
} from '@headlessui/react'
import { Ru, Uz, Us } from 'react-flags-select'
import Cookies from 'js-cookie'
import { useLocale } from 'next-intl'

const flagComponents = {
  ru: Ru,
  uz: Uz,
  en: Us,
} as const

const localeLabel: Record<'ru' | 'uz' | 'en', string> = {
  ru: 'Ru',
  uz: 'Uz',
  en: 'En',
}

type Locale = 'ru' | 'uz' | 'en'

const LanguageDropDownApp: FC = () => {
  const locale = useLocale() as Locale
  const FlagComponent = flagComponents[locale]

  const changeLang = (e: React.MouseEvent, loc: Locale) => {
    e.preventDefault()
    // localePrefix:'never' (Wave 1-4): locale читается из NEXT_LOCALE cookie.
    // После set cookie — reload чтобы Server Component перерендерился с новой locale.
    Cookies.set('NEXT_LOCALE', loc, { expires: 365 })
    window.location.reload()
  }

  return (
    <Menu as="div" className="relative inline-block text-left">
      <div>
        <MenuButton className="inline-flex justify-center w-full px-4 py-2 text-lg font-medium text-secondary bg-white rounded-md hover:bg-gray-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-opacity-75 items-center">
          <FlagComponent />
          <span className="ml-1.5">{localeLabel[locale]}</span>
        </MenuButton>
      </div>
      <Transition
        enter="transition ease-out duration-200"
        enterFrom="opacity-0 translate-y-1"
        enterTo="opacity-100 translate-y-0"
        leave="transition ease-in duration-150"
        leaveFrom="opacity-100 translate-y-0"
        leaveTo="opacity-0 translate-y-1"
      >
        <MenuItems className="absolute right-0 z-10 mt-2 origin-top-right bg-white divide-y divide-gray-100 rounded-md shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none">
          <div className="px-1 py-1">
            {(['uz', 'ru', 'en'] as Locale[]).map((loc) => {
              const Flag = flagComponents[loc]
              return (
                <MenuItem key={loc}>
                  {({ active }) => (
                    <a
                      className={`${
                        active ? 'text-primary' : 'text-gray-900'
                      } group flex rounded-md items-center w-full px-2 py-2 text-lg`}
                      href="#"
                      onClick={(e) => changeLang(e, loc)}
                    >
                      <Flag /> <span className="ml-2">{localeLabel[loc]}</span>
                    </a>
                  )}
                </MenuItem>
              )
            })}
          </div>
        </MenuItems>
      </Transition>
    </Menu>
  )
}

export default memo(LanguageDropDownApp)
```

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/header/LanguageDropDownApp" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add components_new/header/LanguageDropDownApp.tsx
git commit -m "feat(header): add LanguageDropDownApp (NEXT_LOCALE cookie + reload, no Pages router)"
```

---

## Task 5: SignInButtonApp.tsx (placeholder)

**Files:** Create `components_new/header/SignInButtonApp.tsx`

Placeholder кнопка. Использует `useUI().openSignInModal()` — но сама модалка `SignInModal` Wave 2.5c. Сейчас клик не показывает модалку (она не рендерится в App Router context). Кнопка отображается как обычно, без auth flow.

- [ ] **Step 1: Создать файл**

Write `components_new/header/SignInButtonApp.tsx`:

```typescript
'use client'

import { FC, memo, useEffect, useState } from 'react'
import { useUI } from '@components/ui/context'
import { useExtracted } from 'next-intl'

const SignInButtonApp: FC = () => {
  const t = useExtracted()
  const { user, openSignInModal } = useUI()
  const [isClient, setIsClient] = useState(false)

  useEffect(() => {
    setIsClient(true)
  }, [])

  // Server-side и до hydration — рендерим стандартную кнопку (no flicker).
  if (!isClient) {
    return (
      <button
        className="md:bg-gray-200 bg-yellow px-8 py-1 rounded-full text-secondary outline-none focus:outline-none mb-5 md:mb-0 ml-1 md:ml-0 font-bold md:font-normal"
        onClick={openSignInModal}
      >
        {t('Войти')}
      </button>
    )
  }

  // Client: если user есть — показываем имя (упрощено vs UserProfileDropDown).
  // Полный UserProfileDropDown — Wave 2.5c.
  if (user?.user?.name) {
    return (
      <span className="text-secondary px-2 py-1">{user.user.name}</span>
    )
  }

  return (
    <button
      className="md:bg-gray-200 bg-yellow px-8 py-1 rounded-full text-secondary outline-none focus:outline-none mb-5 md:mb-0 ml-1 md:ml-0 font-bold md:font-normal"
      onClick={openSignInModal}
    >
      {t('Войти')}
    </button>
  )
}

export default memo(SignInButtonApp)
```

> **Note:** Клик "Войти" вызывает `openSignInModal()` который только setState'ит флаг в UIContext. Поскольку `SignInModal` не рендерится в App Router (отдельная wave), визуально ничего не изменится при клике. Для пользователя это будет ощущаться как "сломанная кнопка" — но это известный TODO до Wave 2.5c. Пользователь, желающий войти, всё ещё может сделать это через Pages Router URL `/tashkent` (главная) или другие легаси-страницы.

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/header/SignInButtonApp" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add components_new/header/SignInButtonApp.tsx
git commit -m "feat(header): add SignInButtonApp (placeholder — full SignInModal flow in Wave 2.5c)"
```

---

## Task 6: HeaderApp.tsx

**Files:** Create `components_new/HeaderApp.tsx`

Main desktop Header. Composition из созданных в Tasks 2-5 sub-компонентов + logo + Link на главную города.

- [ ] **Step 1: Создать файл**

Write `components_new/HeaderApp.tsx`:

```typescript
'use client'

import React, { FC, useMemo } from 'react'
import Image from 'next/image'
import { Link } from '../i18n/navigation'
import { useUI } from '@components/ui/context'
import ChooseCityDropDownApp from './header/ChooseCityDropDownApp'
import HeaderPhoneApp from './header/HeaderPhoneApp'
import LanguageDropDownApp from './header/LanguageDropDownApp'
import SignInButtonApp from './header/SignInButtonApp'

const HeaderApp: FC = () => {
  const { activeCity, cities } = useUI()

  const chosenCity = useMemo(() => {
    if (activeCity) return activeCity
    if (cities && cities.length) return cities[0]
    return null
  }, [cities, activeCity])

  return (
    <header
      className="py-[15px] items-center md:flex bg-white mb-3"
      id="header"
    >
      <div className="container mx-auto">
        <div className="flex justify-between items-center">
          <div className="w-32 md:w-48 ml-4 md:ml-0">
            <Link href={`/${chosenCity?.slug || ''}`} prefetch={false}>
              <Image
                src="/assets/main_logo.svg"
                width={188}
                height={68}
                alt="main_logo"
              />
            </Link>
          </div>
          <div className="flex items-center">
            <div className="md:flex hidden items-center">
              <HeaderPhoneApp />
              <ChooseCityDropDownApp />
              <div className="mx-2">
                <LanguageDropDownApp />
              </div>
              <div className="mx-2">
                <SignInButtonApp />
              </div>
            </div>
          </div>
        </div>
      </div>
    </header>
  )
}

export default HeaderApp
```

> **Note vs legacy `Header.tsx`:**
> - **Удалено**: `SetLocation` (требует LocationTabs Wave 2.5b), `HeaderMenu` (раскрытое меню Wave 3+), `menuOpen` state и `MenuIcon` toggler.
> - **Удалено**: `fetchConfig()` axios — config теперь грузится server-side в layout (Footer его потребляет).
> - **Mobile вид**: `md:flex hidden` означает что на мобильных весь Header пустой (kроме logo). Mobile UX — Wave 2.5d.

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/HeaderApp" | head -5
```

Expected: empty.

- [ ] **Step 3: Commit**

```bash
git add components_new/HeaderApp.tsx
git commit -m "feat(header): add HeaderApp (composition: logo + city + phone + lang + login)"
```

---

## Task 7: FooterApp.tsx

**Files:** Create `components_new/FooterApp.tsx`

Server component. Принимает props: `categories`, `topMenu`, `footerInfoMenu`, `socials`, `currentCity`, `config`, `locale`. Никаких хуков — pure rendering.

Footer copy из legacy `Layout.tsx` (lines 162-386), упрощённая для server component:
- Удаляем `LinkScroll` (react-scroll, client-only) → заменяем обычными `<Link>` к `/[city]/#productSection_${id}`
- Удаляем `tr()` — заменяем inline RU тексты (Wave 7 build extraction)
- Удаляем `useRouter()` — `pathname` check тоже не нужен (мы не на главной города)

- [ ] **Step 1: Создать файл**

Write `components_new/FooterApp.tsx`:

```typescript
import Image from 'next/image'
import Link from 'next/link'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import {
  faFacebook,
  faInstagram,
  faTelegram,
  IconDefinition,
} from '@fortawesome/free-brands-svg-icons'
import type { APILinkItem } from '@commerce/types/headerMenu'
import type { SocialIcons } from '@commerce/types/socialIcons'
import type { City } from '@commerce/types/cities'
import type { PublicConfig } from '@/lib/data/configs'

const socIcons: Record<string, IconDefinition> = {
  fb: faFacebook,
  inst: faInstagram,
  tg: faTelegram,
}

type Props = {
  categories: any[]
  footerInfoMenu: APILinkItem[]
  socials: SocialIcons[]
  currentCity?: City
  config: PublicConfig
  locale: string
}

export default function FooterApp({
  categories = [],
  footerInfoMenu = [],
  socials = [],
  currentCity,
  config,
  locale,
}: Props) {
  const workTime =
    locale === 'uz'
      ? config.workTimeUz
      : locale === 'en'
        ? config.workTimeEn
        : config.workTimeRu

  const citySlug = currentCity?.slug || ''

  return (
    <footer className="text-white hidden md:flex flex-col">
      <div className="hidden md:block w-full">
        <img
          src="/assets/uzor.svg"
          alt="footer_bg"
          className="w-full h-auto"
        />
      </div>
      <div className="bg-secondary w-full pt-5 pb-2 px-4 md:px-0">
        <div className="container mx-auto md:my-6">
          <div className="md:border-b md:flex justify-between mb-1 md:pb-10">
            <div className="md:w-1/5">
              <div className="hidden md:flex">
                <Image
                  src="/assets/footer_logo.svg"
                  width={188}
                  height={68}
                  alt="footer_logo"
                />
              </div>
              <h3 className="md:block mt-7 text-xl hidden">
                Вкус, который объединяет!
              </h3>
              <div className="md:absolute">
                <a
                  href="https://play.google.com/store/apps/details?id=havoqand.chopar"
                  className="flex"
                  target="_blank"
                  rel="noreferrer"
                >
                  <div className="items-center mt-2">
                    <img
                      src="/googleReady.svg"
                      alt="Get it on Google Play"
                      className="w-48"
                    />
                  </div>
                </a>
                <a
                  href="https://apps.apple.com/uz/app/chopar-pizza/id1597897308"
                  target="_blank"
                  rel="noreferrer"
                >
                  <div className="items-center mt-1">
                    <img
                      src="/appleReady.svg"
                      alt="Available on the App Store"
                      className="w-48"
                    />
                  </div>
                </a>
                <a href="https://telegram.me/Chopar_bot">
                  <div className="bg-black rounded-md w-48 mt-1 h-14 flex items-center justify-evenly">
                    <FontAwesomeIcon icon={faTelegram} className="w-9" />
                    <div className="text-3xl">Telegram</div>
                  </div>
                </a>
              </div>
            </div>
            <div className="flex-grow border-b border-blue md:border-0 mt-5 md:mt-0 pb-5 md:pb-0">
              <div className="md:flex justify-center">
                <div className="mr-24 hidden md:block">
                  <span className="block font-bold mb-3 text-[16px]">Меню</span>
                  <ul className="ml-3">
                    {categories.map((item: any) => (
                      <li
                        key={item.id}
                        className="leading-[1.85rem] cursor-pointer hover:text-yellow"
                      >
                        <Link
                          href={`/${citySlug}/#productSection_${item.id}`}
                          prefetch={false}
                        >
                          {item?.attribute_data?.name?.['chopar']?.[locale] ||
                            item?.attribute_data?.name?.['chopar']?.['ru']}
                        </Link>
                      </li>
                    ))}
                  </ul>
                </div>
                <div>
                  {footerInfoMenu && footerInfoMenu.length > 0 && (
                    <>
                      <span className="block font-bold mb-3 text-[16px]">
                        Информация
                      </span>
                      <ul className="ml-3">
                        {footerInfoMenu.map((item: any) => {
                          const keyTyped =
                            `name_${locale}` as keyof typeof item
                          let href: string = item.href
                          if (href.indexOf('http') < 0) {
                            href = `/${citySlug}${item.href}`
                          }
                          return (
                            <li
                              key={item.href}
                              className="leading-[1.85rem] cursor-pointer hover:text-yellow"
                            >
                              <Link href={href} prefetch={false}>
                                {item[keyTyped] || item.name_ru}
                              </Link>
                            </li>
                          )
                        })}
                      </ul>
                    </>
                  )}
                </div>
              </div>
            </div>
            <div className="md:text-right text-sm leading-7 mt-5 md:mt-0">
              <div className="border-b border-blue md:border-0 pb-5 md:pb-0">
                График работы <br />
                {workTime}
              </div>
              <div className="mt-4 border-b border-blue md:border-0 pb-5 md:pb-0">
                <span>Подписывайтесь на нас:</span>
                <ul className="flex md:justify-end text-4xl">
                  {socials.map((soc) => (
                    <li key={soc.code} className="mx-1">
                      <a
                        target="_blank"
                        className="no-underline text-white"
                        href={soc.link}
                        rel="noreferrer"
                      >
                        {socIcons[soc.code] && (
                          <FontAwesomeIcon
                            icon={socIcons[soc.code]}
                            className="w-10 h-10"
                          />
                        )}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
          <div className="mb-7 md:mb-0">
            {new Date().getFullYear()} Все права защищены
          </div>
        </div>
      </div>
    </footer>
  )
}
```

> **Note:** Использует `import type { PublicConfig } from '@/lib/data/configs'`. Если `@/*` alias не работает (Next.js удаляет его) — заменить на относительный путь `'../lib/data/configs'`. Проверить tsc после написания.

- [ ] **Step 2: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^components_new/FooterApp" | head -5
```

Expected: empty.

If `@/lib/data/configs` not resolved — change import to:
```typescript
import type { PublicConfig } from '../lib/data/configs'
```

- [ ] **Step 3: Commit**

```bash
git add components_new/FooterApp.tsx
git commit -m "feat(footer): add FooterApp (server component, props-driven, no useTranslation)"
```

---

## Task 8: Update app/[city]/layout.tsx + LayoutWrapper.tsx

**Files:**
- Modify: `app/[city]/layout.tsx` (server fetch для config + передача props)
- Modify: `app/[city]/LayoutWrapper.tsx` (рендерить HeaderApp + FooterApp + main)

- [ ] **Step 1: Прочитать текущий app/[city]/layout.tsx**

Run:
```bash
cat "app/[city]/layout.tsx"
```

- [ ] **Step 2: Обновить layout.tsx**

Replace `app/[city]/layout.tsx`:

```typescript
import { notFound } from 'next/navigation'
import { getLocale } from 'next-intl/server'
import { fetchSiteInfo } from '../../lib/data/site-info'
import { fetchPublicConfig } from '../../lib/data/configs'
import LayoutWrapper from './LayoutWrapper'
import type { City } from '@commerce/types/cities'

export const dynamicParams = true

export async function generateStaticParams() {
  const siteInfo = await fetchSiteInfo()
  const cities = (siteInfo as any).cities as City[]
  return cities.map((c) => ({ city: c.slug }))
}

export default async function CityLayout({
  children,
  params,
}: {
  children: React.ReactNode
  params: Promise<{ city: string }>
}) {
  const { city: citySlug } = await params
  const [siteInfo, config, locale] = await Promise.all([
    fetchSiteInfo(),
    fetchPublicConfig().catch(() => ({}) as Awaited<ReturnType<typeof fetchPublicConfig>>),
    getLocale(),
  ])
  const cities = (siteInfo as any).cities as City[]
  const currentCity = cities.find((c) => c.slug === citySlug)
  if (!currentCity) notFound()

  return (
    <LayoutWrapper
      pageProps={{
        cities,
        currentCity,
        categories: siteInfo.categories,
        footerInfoMenu: siteInfo.footerInfoMenu,
        socials: siteInfo.socials,
        config,
        locale,
      }}
    >
      {children}
    </LayoutWrapper>
  )
}
```

> Note: `.catch(() => ({}))` для `fetchPublicConfig` — если backend down, footer всё равно отрендерится без workTime (graceful degradation вместо 500).

- [ ] **Step 3: Обновить LayoutWrapper.tsx**

Replace `app/[city]/LayoutWrapper.tsx`:

```typescript
'use client'

import { useEffect } from 'react'
import { useUI } from '@components/ui/context'
import type { City } from '@commerce/types/cities'
import type { APILinkItem } from '@commerce/types/headerMenu'
import type { SocialIcons } from '@commerce/types/socialIcons'
import type { PublicConfig } from '../../lib/data/configs'
import HeaderApp from '../../components_new/HeaderApp'
import FooterApp from '../../components_new/FooterApp'

type Props = {
  children: React.ReactNode
  pageProps: {
    cities: City[]
    currentCity: City
    categories: any[]
    footerInfoMenu: APILinkItem[]
    socials: SocialIcons[]
    config: PublicConfig
    locale: string
  }
}

export default function LayoutWrapper({ children, pageProps }: Props) {
  const { setActiveCity, setCitiesData } = useUI()

  useEffect(() => {
    if (pageProps.currentCity) {
      setActiveCity(pageProps.currentCity)
    }
    if (pageProps.cities?.length) {
      setCitiesData(pageProps.cities)
    }
  }, [pageProps.currentCity, pageProps.cities, setActiveCity, setCitiesData])

  return (
    <>
      <HeaderApp />
      <main className="container mx-auto py-8">{children}</main>
      <FooterApp
        categories={pageProps.categories}
        footerInfoMenu={pageProps.footerInfoMenu}
        socials={pageProps.socials}
        currentCity={pageProps.currentCity}
        config={pageProps.config}
        locale={pageProps.locale}
      />
    </>
  )
}
```

- [ ] **Step 4: Verify TS**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^app/\[city\]/(layout|LayoutWrapper)" | head -10
```

Expected: empty.

- [ ] **Step 5: Commit (один commit на оба изменения)**

```bash
git add "app/[city]/layout.tsx" "app/[city]/LayoutWrapper.tsx"
git commit -m "feat(app): wire HeaderApp + FooterApp into city layout (with config fetch)"
```

---

## Task 9: Verify dev runtime

**Files:** none

- [ ] **Step 1: Запустить dev**

Run (use 5757 if 5656 occupied):
```bash
lsof -i :5757 2>&1 | head -3 || echo "5757 free"
bunx next dev --webpack -p 5757 > /tmp/wave2.5-dev.log 2>&1 &
echo $! > /tmp/wave2.5-dev.pid
sleep 30
tail -15 /tmp/wave2.5-dev.log
```

Expected: `Ready in <X>ms`, no fatal errors про missing modules или undefined components.

- [ ] **Step 2: Curl /tashkent/about — Header + Footer present**

Run:
```bash
curl -s -o /tmp/wave2.5-about.html -w "HTTP %{http_code}\n" http://localhost:5757/tashkent/about
echo "=== Header markers ==="
grep -ocE "main_logo|HeaderPhone|ChooseCity|Войти" /tmp/wave2.5-about.html
echo "=== Footer markers ==="
grep -ocE "footer_logo|googleReady|appleReady|Подписывайтесь|Все права защищены" /tmp/wave2.5-about.html
echo "=== Content (О нас) ==="
grep -ocE "О нас|ВКУС, КОТОРЫЙ" /tmp/wave2.5-about.html
echo "=== Title ==="
grep -oE "<title>[^<]*</title>" /tmp/wave2.5-about.html | head -1
```

Expected:
- HTTP 200
- Header markers > 0 (logo, phone, city dropdown, login button)
- Footer markers > 0 (footer logo, app stores, social marker, copyright)
- Content "О нас" still present
- Title "О компании Chopar Pizza | Chopar Pizza"

- [ ] **Step 3: Curl /tashkent (Pages Router — должен работать)**

Run:
```bash
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:5757/tashkent
```

Expected: HTTP 200 (легаси Pages Router всё ещё работает для главной).

- [ ] **Step 4: Console check via Chrome DevTools MCP**

If Chrome DevTools MCP available:

```
new_page("http://localhost:5757/tashkent/about")
list_console_messages(types: ["error", "warn"])
```

Expected: только legacy warnings (preload/hydration). Никаких "useRouter not mounted", "Cannot read properties of undefined", etc.

- [ ] **Step 5: Остановить dev**

Run:
```bash
kill $(cat /tmp/wave2.5-dev.pid) 2>/dev/null
sleep 2
lsof -i :5757 2>&1 | head -2 || echo "5757 free"
```

---

## Task 10: Production build smoke

**Files:** none

- [ ] **Step 1: Clean build**

Run:
```bash
rm -rf .next && bun run build > /tmp/wave2.5-prod.log 2>&1
echo "EXIT_CODE=$?"
tail -30 /tmp/wave2.5-prod.log
```

Expected: `EXIT_CODE=0`. Route summary показывает `/[city]/about` в App Router (как Wave 2). Никаких новых TS errors про наши файлы.

---

## Task 11: DevTools MCP visual diff

**Files:** none

Сравнить локальную страницу с продакшн.

- [ ] **Step 1: Запустить dev (если не запущен)**

```bash
bunx next dev --webpack -p 5757 > /tmp/wave2.5-dev2.log 2>&1 &
echo $! > /tmp/wave2.5-dev2.pid
sleep 25
```

- [ ] **Step 2: Snapshot обеих страниц**

Through Chrome DevTools MCP:
```
new_page("https://choparpizza.uz/tashkent/about")
take_snapshot()  # save для сравнения

new_page("http://localhost:5757/tashkent/about")
take_snapshot()  # save для сравнения
```

Compare:
- ✅ Header: logo, phone, city dropdown, language, login button — все present и в правильном порядке
- ✅ Main: "О нас" + 3 параграфа RU текст — same as Wave 2A
- ✅ Footer: app stores, menu категории, информация ссылки, соцсети, copyright — всё видно
- ⚠️ Mobile UX (если viewport < md) — Header пустой кроме logo (это ожидаемо, mobile в Wave 2.5d)

- [ ] **Step 3: Console**

```
list_console_messages(types: ["error"])
```

Expected: пусто или только known issues (favicon 404). Никаких React hydration mismatch errors.

- [ ] **Step 4: Network**

```
list_network_requests()
```

Expected:
- `/api/configs/public` фетчится server-side (не должно быть в client network requests если он cached на server)
- Изображения с CDN
- Никаких 5xx

- [ ] **Step 5: Остановить dev**

```bash
kill $(cat /tmp/wave2.5-dev2.pid) 2>/dev/null
sleep 2
```

---

## Final Verification

- [ ] **Step 1: File tree**

Run:
```bash
ls components_new/HeaderApp.tsx components_new/FooterApp.tsx components_new/header/HeaderPhoneApp.tsx components_new/header/ChooseCityDropDownApp.tsx components_new/header/LanguageDropDownApp.tsx components_new/header/SignInButtonApp.tsx lib/data/configs.ts
```

Expected: все 7 файлов существуют.

- [ ] **Step 2: TS clean for our code**

Run:
```bash
bunx tsc --noEmit 2>&1 | grep -E "^(app/|i18n/|lib/data/|lib/posthog-app|proxy\.ts|components_new/(seo|about|HeaderApp|FooterApp|header/(HeaderPhone|ChooseCity|Language|SignInButton)App))" | head -10
```

Expected: пустой вывод.

- [ ] **Step 3: Build OK**

Run:
```bash
rm -rf .next && bun run build > /tmp/wave2.5-final.log 2>&1
echo "EXIT_CODE=$?"
```

Expected: `EXIT_CODE=0`.

- [ ] **Step 4: Commits**

Run:
```bash
git log --oneline 79d78ae3..HEAD | head -15
```

Expected: ~9-11 commits Wave 2.5 (lib/data/configs → HeaderPhoneApp → ChooseCityDropDownApp → LanguageDropDownApp → SignInButtonApp → HeaderApp → FooterApp → wire layout/wrapper).

- [ ] **Step 5: Branch clean**

```bash
git status
```

Expected: clean (или только known autogen files).

---

## Wave 2.5 Done. Что дальше:

- **Wave 2B** теперь возможен (статичные contacts/delivery/branch/privacy/fran с рабочим Header+Footer)
- **Wave 2.5b** — LocationTabs + SetLocation (Leaflet map для выбора адреса доставки)
- **Wave 2.5c** — SignInModal + UserProfileDropDown (auth flow OTP)
- **Wave 2.5d** — MobileHeader + MobileBottomNav + Mobile Location/SignIn

---

## Self-Review Checklist (для разработчика)

- [ ] Каждая Task имеет `Files:` секцию с exact paths
- [ ] Каждый Step имеет либо exact код, либо exact команду
- [ ] **Никаких `useTranslations`/`getTranslations`** — только `useExtracted`/`getExtracted`
- [ ] Все Header sub-компоненты — `'use client'` (потому что useUI/useState/etc.)
- [ ] FooterApp — server component (без `'use client'`)
- [ ] `useRouter` используется только из `@/i18n/navigation`, не из `next/router`
- [ ] `useLocale` используется из `next-intl` (не `useRouter().locale`)
- [ ] City switching работает через URL navigation (router.push новый path)
- [ ] Language switching работает через NEXT_LOCALE cookie + reload (потому что localePrefix:'never')
- [ ] Build `bun run build` exit 0
- [ ] DevTools MCP подтверждает Header+Footer present
